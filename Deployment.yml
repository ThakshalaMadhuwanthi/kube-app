apiVersion: apps/v1
kind: Deployment
metadata: 
  name: kube-app-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kube-app
  template:
    metadata:
      labels:
        app: kube-app
    spec:
      containers:
        - name: kube-app-container
          image: kube-app:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80